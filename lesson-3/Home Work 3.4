"""
Задание 4
Программа принимает действительное положительное число x и целое отрицательное число y. Выполните возведение числа x
в степень y. Задание реализуйте в виде функции my_func(x, y). При решении задания нужно обойтись без встроенной функции
возведения числа в степень.
"""


def my_func(x, y):
    return x ** y


def my_func2(x, y):
    y = abs(y)
    result = x
    for i in range(y + 1):
        result /= x
    return result


a = float(input("Введите любое действительное число: "))
b = int(input("Введите любое целое отрицательное число: "))

print(f"\nРешение с помощью оператора: " + '{:.6f}'.format(my_func(a, b)))
print(f"\nРешение с помощью модуля:    " + '{:.6f}'.format(my_func2(a, b)))
