"""
Задание 6
Реализовать структуру данных «Товары». Она должна представлять собой список кортежей. Каждый кортеж хранит информацию
об отдельном товаре. В кортеже должно быть два элемента — номер товара и словарь с параметрами (характеристиками товара:
название, цена, количество, единица измерения). Структуру нужно сформировать программно, т.е. запрашивать все
данные у пользователя.
"""
# Для быстрой проверки аналитики:
'''
list_products_structure = [(1, {"Название": "компьютер", "Цена": 20000, "Количество": 5, "eд": "шт."}), 
                           (2, {"Название": "принтер", "Цена": 6000, "Количество": 2, "eд": "шт."}), 
                           (3, {"Название": "сканер", "Цена": 2000, "Количество": 7, "eд": "шт."})]
'''
list_products_structure = list()

for i in range(1, 4):

    dict_product = {"Название": input("\nВведите название товара: ")}
    dict_product.update({"Цена": int(input("Цена товара: "))})
    dict_product.update({"Количество": int(input("Количество: "))})
    dict_product.update({"ед.": input("Еденица измерения: ")})

    tuple_product = (i, dict_product)

    list_products_structure.append(tuple_product)

print("\nГотовая структура:", *list_products_structure, sep="\n")

dict_product_analytics = {"Название": [], "Цена": [], "Количество": [], "ед.": []}

for tup in list_products_structure:
    for k, v in tup[1].items():
        for k1, v1 in dict_product_analytics.items():
            if k == k1 and v not in v1:
                v1.append(v)

print("\nАналитика товаров:", *dict_product_analytics.items(), sep="\n")
